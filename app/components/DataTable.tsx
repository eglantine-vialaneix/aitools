"use client"
/*
const [columns, setColumns] = useState([
  { id: "name", label: "Name" },
  { id: "role", label: "Role" },
]);   

const addColumn = (newColumn) => {
  setColumns((prev) => [...prev, newColumn]);
};   

<Table aria-label="Dynamic Table">
  <Table.Header>
    {columns.map((column) => (
      <Table.Column key={column.id}>{column.label}</Table.Column>
    ))}
  </Table.Header>
  <Table.Body>
    {...} // Render raw based on your data
  </Table.Body>
</Table>   

*/